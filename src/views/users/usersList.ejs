<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head') %>

<body id="pd-body">
    <%- include('../partials/header') %>

  
    <main class="usersl-main">
        <% for(let i = 0 ; i < users.length; i++){ %>
            <a class="usersl-main-article" href="/users/profile/<%= users[i].id %>">
                <div class="usersl-divs">
                    <div id="usersl-avatar-box">
                        <img src="/img/avatars/<%= users[i].avatar %>" alt="avatar" class="avatar">
                    </div>  
                </div>
                <div class="usersl-divs">
                    <h1><%= users[i].userName %></h1>
                    <h2><%= users[i].name %></h2>
                    <p><%= users[i].email %></p>
                    <h3>id: <%= users[i].id %></h3>
                </div>                           
            </a>
        <% } %>     
    </main>

    <%- include('../partials/footer') %>
</body>
</html>